# TodayEng Frontend 개발 AI 지침

이 문서는 이 저장소에서 작업하는 모든 개발 AI가 따라야 하는 규칙입니다.
저장소 전체에 적용되며, 사용자 요청이나 더 하위 경로의 `AGENTS.md`에 별도 지침이 있는 경우 해당 지침을 우선합니다.

## 프로젝트 기본 정보

- React 19, TypeScript, Vite, Tailwind CSS 기반의 프론트엔드 프로젝트입니다.
- 기능별 코드는 `src/features/{기능명}` 아래에 둡니다.
- 라우트 진입점은 얇게 유지하고, 실제 UI와 로직은 각 기능 폴더에 배치합니다.
- 절대 경로 import는 `@/` 별칭을 사용합니다.

## 공통 코드 보호 규칙

### `src/shared`는 수정 금지

- `src/shared/**`의 기존 파일은 절대 수정, 삭제, 이동, 이름 변경하지 않습니다.
- 공통 컴포넌트의 props, 스타일, 동작을 임의로 변경하지 않습니다.
- 기존 공통 컴포넌트가 요구사항과 조금 다르더라도 특정 기능을 위해 공통 컴포넌트를 변경하지 않습니다.
- 공통 컴포넌트 수정이 반드시 필요하다고 판단되면 직접 수정하지 말고, 필요한 변경과 영향 범위를 사용자에게 설명한 뒤 명시적인 허락을 받습니다.

### 공통 컴포넌트 우선 재사용

- 버튼, 모달, 헤더, 하단 내비게이션, 텍스트 레이아웃, 공통 아이콘을 새로 만들기 전에 `src/shared/components`를 먼저 확인합니다.
- 사용할 수 있는 공통 컴포넌트가 있으면 반드시 import해서 사용합니다.
- 공통 컴포넌트를 복사하여 기능 폴더에 중복 구현하지 않습니다.
- 기능별 조합이나 래퍼가 필요하면 `src/features/{기능명}/components`에 새 컴포넌트를 만들고, 내부에서 공통 컴포넌트를 조합합니다.
- 공통 컴포넌트의 내부 스타일을 우회하기 위한 과도한 CSS 덮어쓰기는 피합니다.

현재 주요 공통 컴포넌트:

- `Button`
- `ButtonPair`
- `Modal`
- `TextLayout`
- `BackHeaderLayout`
- `BottomNav`
- `src/shared/components/icons` 아래의 공통 아이콘

### 새로운 공통 코드 추가

- 한 기능에서만 사용하는 코드는 `src/shared`에 추가하지 않습니다.
- 여러 기능에서 실제로 재사용할 필요가 확인된 경우에만 공통화를 제안합니다.
- AI는 사용자 허락 없이 `src/shared`에 새 파일을 추가하지 않습니다.

## 기능 코드 작성 규칙

- 새 기능은 `src/features/{기능명}` 아래에 작성합니다.
- 기능 내부에서는 필요에 따라 다음 구조를 사용합니다.

```text
src/features/{기능명}/
├── api/
├── components/
├── hooks/
├── pages/
├── types.ts
└── utils/
```

- 한 기능에서만 쓰는 컴포넌트, 훅, 타입, 유틸은 해당 기능 폴더 밖으로 이동하지 않습니다.
- 다른 기능 폴더의 내부 구현을 직접 import하지 않습니다. 공통화가 필요한 경우 사용자에게 먼저 제안합니다.
- 페이지 컴포넌트에는 복잡한 계산이나 데이터 변환을 쌓지 말고 기능별 훅 또는 유틸로 분리합니다.

## 스타일 및 UI 규칙

- 기존 Tailwind CSS 클래스와 `tailwind.config.js`의 디자인 토큰을 우선 사용합니다.
- 이미 정의된 색상, 타이포그래피, 간격 토큰이 있으면 임의의 값을 새로 만들지 않습니다.
- 전역 스타일인 `src/index.css`는 사용자 허락 없이 수정하지 않습니다.
- 기존 화면의 최대 너비, 모바일 레이아웃, 내비게이션 영역을 유지합니다.
- 아이콘과 이미지가 이미 `src/assets` 또는 `public`에 있으면 새 자산을 만들기 전에 재사용 가능 여부를 확인합니다.
- 접근성을 위해 이미지에 적절한 `alt`를 제공하고, 장식용 이미지는 `alt=""` 또는 `aria-hidden="true"`를 사용합니다.
- 클릭 동작에는 가능한 한 의미에 맞는 `button`, `a`, `NavLink` 요소를 사용합니다.

## TypeScript 및 React 규칙

- `any` 사용을 피하고 props, API 응답, 상태에 명시적인 타입을 정의합니다.
- 타입만 import할 때는 `import type`을 사용합니다.
- 불필요한 상태와 Effect를 만들지 않습니다.
- 파생 가능한 값은 별도 상태로 저장하지 않습니다.
- 컴포넌트는 하나의 명확한 책임을 갖도록 작게 유지합니다.
- 기존 코드의 export 방식과 파일 명명 규칙을 따릅니다.
- 새 외부 패키지는 사용자 허락 없이 설치하지 않습니다.

## 라우팅 및 API 규칙

- 라우트 변경은 `src/routes/AppRoutes.tsx`의 기존 구조를 유지합니다.
- 기존 경로를 임의로 변경하거나 제거하지 않습니다.
- API 호출 코드는 UI 컴포넌트에서 분리하여 해당 기능의 API 모듈에 둡니다.
- 토큰, 비밀번호, API 키, 개인 정보와 같은 비밀 값을 코드에 하드코딩하지 않습니다.
- 실제 API가 준비되지 않은 경우 mock 데이터임을 코드에서 명확하게 구분합니다.

## 작업 범위 및 안전 규칙

- 요청받은 기능과 직접 관련된 파일만 수정합니다.
- 사용자의 기존 변경 사항을 되돌리거나 덮어쓰지 않습니다.
- 대규모 리팩터링, 폴더 이동, 파일 이름 변경은 별도 요청 없이 수행하지 않습니다.
- 생성 파일, 빌드 산출물, 의존성 폴더를 커밋 대상 소스처럼 수정하지 않습니다.
- 요구사항이 공통 코드 수정 없이는 충족되기 어렵다면 임의로 우회하지 말고 사용자에게 영향과 선택지를 알립니다.

## 작업 완료 전 검증

변경 후 가능한 범위에서 다음 명령을 실행합니다.

```bash
npm run lint
npm run format:check
npm run build
```

- 검사 실패 시 이번 변경으로 발생한 문제인지 기존 문제인지 구분해서 보고합니다.
- 경고나 오류를 숨기기 위해 린트 규칙, TypeScript 설정, 빌드 설정을 완화하지 않습니다.
- 검증하지 못한 항목이 있으면 완료 보고에 그 이유를 명시합니다.

## AI 작업 보고

- 수정한 파일과 핵심 변경 내용을 간결하게 설명합니다.
- 실행한 검증 명령과 결과를 함께 알립니다.
- `src/shared`, 전역 스타일, 라우팅, 설정 파일에 영향을 줄 가능성이 있으면 작업 전에 사용자에게 먼저 알립니다.
